defmodule ReimbursementMonitor.Polling.JobAgentTest do
  use ExUnit.Case
  doctest ReimbursementMonitor.Polling.JobAgent

  alias ReimbursementMonitor.Polling.JobAgent

  # TODO: write a couple simple tests to exercise the agent. This will require
  # createing a job and putting it into the agent, and getting it back out.
  # This allows us to update the cron string on the job as needed
  test "a test" do 
    true
  end
end